import ShareMealPage from "../meals/share/page";

export const metadata = {
  title: "🌽 SimplyFood",
  description: "Share recipe",
};

const ShareRecipe = () => {
  return (
    <main>
      <ShareMealPage />
    </main>
  );
};

export default ShareRecipe;
